defmodule Hangman.Lib.Dataparsertest do
  use ExUnit.Case
  alias Hangman.Lib.Dataparser
  doctest Hangman

  test "data_parse" do
  	#data_rows = Dataparser.parse_data
  	#first_row = Enum.at(data_rows,0)
  	#IO.inspect first_row
  	
  end
end
